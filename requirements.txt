# Continuity — requirements
# Install with: pip install -r requirements.txt

# Core — required
chromadb>=0.4.0
anthropic>=0.25.0

# MCP server support
mcp>=0.9.0

# Scheduling (nightly pipeline)
schedule>=1.2.0

# System monitoring (optional — used by some automation scripts)
psutil>=5.9.0

# HTTP requests (research scripts, webhook support)
requests>=2.31.0

# Windows service support (optional — Task Scheduler integration)
# pywin32>=306  # Uncomment if wiring into Windows Task Scheduler
